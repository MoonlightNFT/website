# Moonlight Website

## Usage
First, install all necessary dependencies (in the root folder)
```shell
npm install
```

Then, run the web app on `localhost:3000`
```shell
npm start
```

## Description

